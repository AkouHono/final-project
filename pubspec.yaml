name: eduflash
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Updated Firebase dependencies (compatible with Flutter 3.9.x)
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.4  # Updated Firestore

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
